strCopies 

public boolean strCopies(String str, String sub, int n) {
  if (n > 0) { 
    int x = str.indexOf(sub) ; 
    if (x != -1) { 
      return  strCopies(str.substring(x+1), sub, n - 1 ) ; 
    } 
    return false ; 
  }
  return true ; 
     
}
